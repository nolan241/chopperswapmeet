<%= render 'pages/home' %>
<div class="container-fluid">
    <%= render partial: 'pin', locals: { pins: @pins } %>

</div>

<div class="center">
  <%= will_paginate @pins, renderer: BootstrapPagination::Rails %>
</div>